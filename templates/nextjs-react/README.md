# __PROJECT_NAME__

Generado a partir de la plantilla `nextjs-react` del monorepo `template`.

## Desarrollo

    pnpm install
    pnpm dev

## Tests

    pnpm test

## Build & Docker

    pnpm build
    docker compose up --build
